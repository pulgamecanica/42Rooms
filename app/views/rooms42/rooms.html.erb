<div class="rooms-centered">
    <section class="rooms-cards">
        <% if current_user %><div class="campus-card"><h1><%= current_user.campus %></h1></div><% end %>
        <% @rooms.each do |room| %>
            <article class="room-card-small">
                <%= link_to room_calendar_path(room) do %><h2><%=room.name%></h2><h4>Capacity: <%=room.capacity%> - <%=room.description%></h4><% end %>
                <details>
                    <summary>Show Reservations (<%= room.reservations.active.all.count %>)</summary>
                    <% room.reservations.active.order(starts_at: :asc).each do |res| %>
                        <ul><li><%= res %></li></ul>
                    <% end %> 
                </details>
            </article>
        <% end %>
    </section>
</div>